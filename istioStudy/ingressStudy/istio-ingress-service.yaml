apiVersion: v1
kind: Service
metadata:
  name: istio-envoy-service
  namespace: istio-system
spec:
  type: ClusterIP
  selector:
    app: istio-ingressgateway
  ports:
  - name: istio-envoy-service
    protocol: TCP
    port: 15000
    targetPort: 15000



